/*import axios from 'axios';
 
const API_URL = 'http://localhost:8083/api/v1.0/auth';
 
export const doLogin = async (loginRequest) => {
  try {
    const response = await axios.post(`${API_URL}/login`, loginRequest);
    return response.data;
  } catch (error) {
    throw error.response.data; // Assuming the error response contains the relevant data
  }
};
*/